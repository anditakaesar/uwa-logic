{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Uwa-Logic local Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            // "preLaunchTask": "buildenvfile",
            // "envFile": "${workspaceFolder}/.env",
        }
    ]
}